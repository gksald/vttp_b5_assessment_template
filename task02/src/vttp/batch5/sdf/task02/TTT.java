package vttp.batch5.sdf.task02;

import java.util.ArrayList;
import java.util.List;

public class TTT {

    Boolean userDraw = true;
    Boolean playerWin = true;
    String[] possiblePositions = {"0,0","0,1","0,2","1,0","1,1","1,2","2,0","2,1","2,2"};



    }
    // pseudo code
    // use a for loop to get utilityValues in Utility class for every possiblePositions.
    // if userDraw=false and if userWin=true, utilityValue = 1
    // if userDraw=false and if userWin=false, utilityValue = -1
    // else if userDraw=true, utilityValue = 0
    if (userDraw = false) {
        if (userWin = true) {
            utilityValue = 1;
        } else (utilityValue = -1);
    } else if userWin=true, utilityValue = 1
    

    



